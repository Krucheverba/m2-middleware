# Инструкция по настройке M2 Middleware

## ✅ Токены добавлены

Ваши токены уже добавлены в файл `.env`:

- ✅ `MS_TOKEN` - МойСклад токен установлен
- ✅ `YANDEX_TOKEN` - Яндекс токен установлен
- ⚠️ `YANDEX_CAMPAIGN_ID` - **ТРЕБУЕТСЯ ДОБАВИТЬ**

## 📝 Что нужно сделать

### 1. Добавить YANDEX_CAMPAIGN_ID

Откройте файл `.env` и замените `YOUR_CAMPAIGN_ID_HERE` на ваш реальный Campaign ID из Яндекс.Маркет:

```env
YANDEX_CAMPAIGN_ID=ваш_реальный_campaign_id
```

**Где найти Campaign ID:**
- Зайдите в личный кабинет Яндекс.Маркет
- Откройте настройки магазина M2
- Campaign ID обычно виден в URL или в настройках API

### 2. Проверить конфигурацию

После добавления Campaign ID проверьте что всё работает:

```bash
node -e "const config = require('./src/config'); console.log('✅ Конфигурация загружена');"
```

Если команда выполнилась без ошибок - всё настроено правильно!

### 3. Запустить сервер

```bash
npm start
```

Сервер запустится на порту 3000 (или на порту указанном в `PORT`).

### 4. Проверить работу

```bash
# Health check
curl http://localhost:3000/health

# Должен вернуть:
# {"status":"ok","timestamp":"..."}
```

## 🔐 Безопасность

**ВАЖНО:** Файл `.env` содержит секретные токены!

- ✅ Файл `.env` уже добавлен в `.gitignore`
- ❌ **НЕ коммитьте** `.env` в git
- ❌ **НЕ делитесь** токенами публично

## 📋 Текущая конфигурация

Ваш файл `.env` содержит:

```env
PORT=3000
YANDEX_CAMPAIGN_ID=YOUR_CAMPAIGN_ID_HERE  # ⚠️ ЗАМЕНИТЕ НА РЕАЛЬНЫЙ
YANDEX_TOKEN=y0__xD7rP4QGKOEPCCalqq_FTCgt7WvCIs53Z0n_GqHkK_ZXQYwUzjpK2X0
MS_TOKEN=fda4f28da58c5eb73775a6183857a65f642b04c6
MS_BASE=https://api.moysklad.ru/api/remap/1.2
STOCK_SYNC_INTERVAL_MINUTES=10
ORDER_POLL_INTERVAL_MINUTES=5
LOG_LEVEL=info
```

## 🚀 Следующие шаги

После настройки Campaign ID:

1. **Запустить сервер** - `npm start`
2. **Настроить webhook в МойСклад**:
   - URL: `https://your-domain.com/webhook/moysklad`
   - События: product, stock, enter, loss, move, customerorder, demand
3. **Мониторить логи** - `tail -f logs/combined.log`

## 🧪 Тестирование

Запустить все тесты:

```bash
./run-all-tests.sh
```

Или отдельные тесты:

```bash
node test-mapper.js
node test-stock-service.js
node test-order-service.js
```

## 📚 Документация

- `PROJECT_COMPLETION_SUMMARY.md` - Полная сводка проекта
- `TEST_RESULTS_SUMMARY.md` - Результаты тестов
- `docs/` - Детальная документация по каждому компоненту

## ❓ Проблемы

### Ошибка "Missing required configuration"

Убедитесь что все обязательные параметры заполнены:
- `YANDEX_CAMPAIGN_ID` - не должен быть пустым
- `YANDEX_TOKEN` - должен быть установлен
- `MS_TOKEN` - должен быть установлен

### Ошибка при запуске

Проверьте логи:
```bash
cat logs/error.log
```

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте документацию в папке `docs/`
2. Посмотрите примеры в тестовых файлах `test-*.js`
3. Проверьте логи в папке `logs/`

---

**Готово к работе после добавления YANDEX_CAMPAIGN_ID!** 🎉
