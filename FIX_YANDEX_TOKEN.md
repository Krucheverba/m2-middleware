# 🔧 Исправление проблемы с YANDEX_TOKEN

## Проблема
Текущий `YANDEX_TOKEN` в файле `/root/m2-middleware/.env` **не имеет прав доступа** к Campaign ID `198473170` (магазин M2).

При попытке получить заказы получаем ошибку:
```
403 Forbidden - Access denied
```

## Причина проблемы

OAuth токен привязан к пользователю. Если ваш email не добавлен как пользователь компании с Campaign ID 198473170, то токен не будет иметь прав доступа.

## Решение

### Вариант 1: Проверить к каким Campaign ID имеет доступ текущий токен

Скопируйте файл `check-token-campaigns.js` на сервер и запустите:
```bash
scp check-token-campaigns.js root@89.223.125.212:/root/m2-middleware/
ssh root@89.223.125.212
cd /root/m2-middleware
node check-token-campaigns.js
```

Это покажет список всех Campaign ID, к которым имеет доступ текущий токен.

### Вариант 2: Попросить владельца добавить вас

Владелец магазина M2 (Campaign ID 198473170) должен:
1. Зайти в **Яндекс.Партнёрский интерфейс**
2. Перейти в **Настройки → Пользователи**
3. Добавить ваш email с правами:
   - Чтение заказов
   - Управление остатками
   - Управление ценами
4. После этого вы сможете создать токен под своим аккаунтом

### Вариант 3: Использовать токен владельца

Попросите владельца магазина M2:
1. Зайти в **Настройки → API** в своём аккаунте Яндекс.Маркет
2. Создать OAuth токен
3. Передать вам этот токен

### Шаг 1: Получить правильный токен
После того как вас добавили в пользователи или получили токен от владельца:
1. Зайдите в **Яндекс.Партнёрский интерфейс** для магазина M2
2. Перейдите в **Настройки → API**
3. Создайте новый OAuth токен или используйте полученный
4. **ВАЖНО**: Убедитесь что токен имеет доступ к Campaign ID `198473170`

### Шаг 2: Обновить токен на сервере
Выполните команду (замените `YOUR_NEW_TOKEN` на ваш токен):

```bash
ssh root@89.223.125.212
cd /root/m2-middleware
nano .env
```

Найдите строку:
```
YANDEX_TOKEN=y0__xD7rP4QGKOEPCCalqq_FTCgt7WvCIs53Z0n_GqHkK_ZXQYwUzjpK2X0
```

Замените на:
```
YANDEX_TOKEN=YOUR_NEW_TOKEN
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

### Шаг 3: Перезапустить сервер
```bash
pm2 restart m2-middleware
```

### Шаг 4: Проверить что токен работает
```bash
cd /root/m2-middleware
node test-polling.js
```

Должно быть:
```
✅ Получено X заказов в статусе PROCESSING
```

Вместо:
```
❌ Request failed with status code 403
```

## Текущее состояние
- ✅ Сервер работает
- ✅ Webhook настроен
- ✅ Маппинги загружены (101 шт)
- ✅ Логи Winston теперь пишутся в файлы
- ❌ **YANDEX_TOKEN неправильный** - нужно обновить

## После исправления токена
После обновления токена:
1. Polling заказов начнёт работать автоматически (каждые 5 минут)
2. Webhook события от Яндекс.Маркет будут обрабатываться
3. Заказы будут создаваться в МойСклад

## Проверка после исправления
```bash
# Проверить что polling работает
ssh root@89.223.125.212 "tail -f /root/m2-middleware/logs/combined.log"

# Создать тестовый заказ на M2
# Через 5 минут он должен появиться в МойСклад
```
