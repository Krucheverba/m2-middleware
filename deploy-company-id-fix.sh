#!/bin/bash

# 🚀 Скрипт деплоя исправления MS_COMPANY_ID на сервер
# Использование: bash deploy-company-id-fix.sh

set -e

echo "🚀 Деплой исправления MS_COMPANY_ID на сервер..."
echo ""

SERVER="root@89.223.125.212"
APP_DIR="/root/m2-middleware"
APP_NAME="m2-middleware"

# Цвета
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo "📋 ШАГ 1: Подключение к серверу и обновление кода..."
ssh $SERVER << 'ENDSSH'
cd /root/m2-middleware

# Обновляем код
echo "📥 Получаем последние изменения..."
git pull

# Проверяем текущий .env
echo ""
echo "📋 ШАГ 2: Проверка .env файла..."
if grep -q "MS_COMPANY_ID" .env; then
    echo "✅ MS_COMPANY_ID уже есть в .env"
else
    echo "⚠️  Добавляем MS_COMPANY_ID в .env..."
    # Добавляем MS_COMPANY_ID после MS_TOKEN
    sed -i '/MS_TOKEN=/a MS_COMPANY_ID=144131919' .env
    echo "✅ MS_COMPANY_ID добавлен"
fi

echo ""
echo "📋 ШАГ 3: Текущая конфигурация МойСклад:"
grep "MS_" .env

echo ""
echo "📋 ШАГ 4: Перезапуск приложения..."
pm2 restart m2-middleware

echo ""
echo "⏳ Ждем 3 секунды..."
sleep 3

echo ""
echo "📋 ШАГ 5: Проверка статуса..."
pm2 list | grep m2-middleware

echo ""
echo "📋 ШАГ 6: Последние логи:"
pm2 logs m2-middleware --lines 20 --nostream

ENDSSH

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo -e "${GREEN}✅ Деплой завершен!${NC}"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "📝 Что было сделано:"
echo "  ✅ Обновлен код с GitHub"
echo "  ✅ Добавлен MS_COMPANY_ID=144131919 в .env"
echo "  ✅ Перезапущено приложение"
echo ""
echo "🔍 Для просмотра логов:"
echo "  ssh $SERVER 'pm2 logs m2-middleware'"
echo ""
