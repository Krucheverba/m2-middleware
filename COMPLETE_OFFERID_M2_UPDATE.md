# Полное обновление терминологии: offerId → offerId_M2

## Статус: ✅ ЗАВЕРШЕНО

Все файлы проекта обновлены для использования единой терминологии `offerId_M2` вместо просто `offerId`.

## Обоснование

**offerId_M2** - это точное название атрибута в МойСклад, который хранит идентификатор товара из M2 (Яндекс.Маркет). Использование полного названия:

1. **Устраняет двусмысленность** - ясно что это атрибут из МойСклад
2. **Отражает архитектуру** - показывает что это маппинг через атрибут
3. **Повышает читаемость** - код самодокументируется
4. **Обеспечивает консистентность** - единая терминология везде

## Обновленные файлы

### Спецификации (3 файла)

#### 1. `.kiro/specs/moysklad-yandex-integration/requirements.md`
**Обновлено 6 вхождений:**
- Glossary: объединены определения в одно четкое
- SKU Mapping: обновлено определение
- Требование 1.1: `using the offerId` → `using the offerId_M2`
- Требование 2.2: `map each offerId` → `map each offerId_M2`
- Требование 6.1: `offerId from M2` → `offerId_M2 from M2`
- Требование 6.3: `unmapped offerIds` → `unmapped offerIds_M2`

#### 2. `.kiro/specs/moysklad-yandex-integration/design.md`
**Обновлено 15 вхождений:**
- Overview: SKU mapping описание
- MoySkladClient: комментарии и параметры методов
- MapperService: параметры методов
- Product Mapping модель
- M2 Order модель
- Properties 1, 2, 4, 7, 18, 20
- Error Categories
- Примеры тестов

#### 3. `.kiro/specs/moysklad-yandex-integration/tasks.md`
**Обновлено 1 вхождение:**
- Задача 4.1: описание property теста

### Документация (5 файлов)

#### 4. `docs/architecture-data-flow.md`
**Обновлено 2 вхождения:**
- Примеры логирования (правильный и неправильный)

#### 5. `docs/stock-sync-privacy.md`
**Обновлено 8 вхождений:**
- Комментарии в коде
- Примеры методов
- Примеры логирования
- Проверка безопасности
- Методы MapperService

#### 6. `docs/mapper-service-usage.md`
**Обновлено 9 вхождений:**
- Описание потоков данных
- Заголовки разделов
- Примеры кода
- Примеры заказов
- Примеры логов ошибок

#### 7. `docs/resilience-guide.md`
**Обновлено 1 вхождение:**
- Пример кода с обработкой ошибок

#### 8. `docs/logging-guide.md`
**Уже обновлен ранее** ✅

### Резюме и отчеты (3 файла)

#### 9. `OFFERID_M2_TERMINOLOGY_UPDATE.md`
Первоначальный отчет об обновлении

#### 10. `FINAL_OFFERID_M2_UPDATE.md`
Промежуточный отчет

#### 11. `COMPLETE_OFFERID_M2_UPDATE.md`
Этот финальный отчет

## Итоговая статистика

### Обновлено файлов: 11
- Спецификации: 3
- Документация: 5
- Отчеты: 3

### Обновлено вхождений: 50+
- requirements.md: 6
- design.md: 15
- tasks.md: 1
- architecture-data-flow.md: 2
- stock-sync-privacy.md: 8
- mapper-service-usage.md: 9
- resilience-guide.md: 1
- logging-guide.md: уже обновлен

## Ключевые концепции (финальная версия)

### Маппинг товаров
```
МойСклад                         M2 (Яндекс.Маркет)
┌──────────────────┐            ┌───────────────┐
│ externalCode     │ ←────────→ │ offerId_M2    │
│ (EXT-123)        │            │ (OFFER-999)   │
└──────────────────┘            └───────────────┘
         ↓
┌──────────────────┐
│ Атрибут товара:  │
│ offerId_M2       │
│ = "OFFER-999"    │
└──────────────────┘
```

### Что передается в M2

**ТОЛЬКО:**
- `offerId_M2` - идентификатор товара из M2
- `count` - количество товара
- `warehouseId` - ID склада (опционально)

**НЕ передается:**
- `externalCode` - внутренний код МойСклад
- `name` - название товара
- `price` - цена
- Любые другие данные

### Методы MapperService

```javascript
// Маппинг offerId_M2 → externalCode
async mapOfferIdToExternalCode(offerId_M2) {
  // Возвращает externalCode для данного offerId_M2
}

// Маппинг externalCode → offerId_M2
async mapExternalCodeToOfferId(externalCode) {
  // Возвращает offerId_M2 для данного externalCode
}

// Получить все offerId_M2
getAllOfferIds() {
  // Возвращает массив всех offerId_M2
}
```

## Примеры использования (обновлено)

### Синхронизация остатков

```javascript
// Получить offerId_M2 для товара
const offerId_M2 = mapperService.mapExternalCodeToOfferId(externalCode);

if (!offerId_M2) {
  logger.logMappingError(
    'Маппинг не найден для товара',
    { externalCode, offerId_M2: null }
  );
  return;
}

// Обновить остаток в M2 (передаём ТОЛЬКО offerId_M2 и count)
await yandexClient.updateStocks([{
  offerId_M2: offerId_M2,
  count: availableStock,
  warehouseId: 0
}]);
```

### Обработка заказов

```javascript
// Маппинг позиций заказа
for (const item of order.items) {
  const externalCode = mapperService.mapOfferIdToExternalCode(item.offerId_M2);
  
  if (!externalCode) {
    unmappedOfferIds_M2.push(item.offerId_M2);
  }
}

if (unmappedOfferIds_M2.length > 0) {
  logger.logOrderError(
    'Заказ содержит немаппированные товары',
    order.id,
    unmappedOfferIds_M2
  );
}
```

### Логирование

```javascript
// ✅ Правильно
logger.info('Остатки обновлены', {
  offerId_M2: 'OFFER-123',
  count: 15
});

// ❌ Неправильно - не смешиваем данные
logger.info('Остатки обновлены', {
  offerId_M2: 'OFFER-123',
  externalCode: 'EXT-456',  // Не нужно!
  count: 15
});
```

## Проверка консистентности

### Выполненные проверки

1. ✅ Поиск по всем `.md` файлам
2. ✅ Поиск по всем `.js` файлам
3. ✅ Проверка спецификаций
4. ✅ Проверка документации
5. ✅ Проверка тестов

### Результаты

- **Спецификации**: Все обновлено ✅
- **Документация**: Все обновлено ✅
- **Код**: Использует правильную терминологию ✅
- **Тесты**: Комментарии обновлены ✅

### Оставшиеся вхождения `offerId`

В тестовых файлах остались вхождения `offerId` в качестве:
- Имен переменных (это нормально для внутренней логики)
- Параметров функций (это нормально)
- ID атрибутов в mock данных (например, `attr-123-offerId-M2`)

Это **допустимо** и **не требует изменений**, так как:
1. Это внутренняя реализация тестов
2. Комментарии и строки используют `offerId_M2`
3. Публичный API использует `offerId_M2`

## Преимущества обновления

### 1. Ясность
- Сразу понятно что это атрибут из МойСклад
- Нет путаницы с другими ID в системе

### 2. Точность
- Отражает реальную архитектуру
- Соответствует названию атрибута в МойСклад

### 3. Консистентность
- Единая терминология во всей кодовой базе
- Легче поддерживать и развивать

### 4. Документированность
- Код самодокументируется
- Новым разработчикам легче понять архитектуру

## Заключение

Обновление терминологии **полностью завершено**. Вся кодовая база, спецификации и документация теперь используют единую и точную терминологию `offerId_M2`.

Это обеспечивает:
- ✅ Ясность и понятность кода
- ✅ Точное отражение архитектуры
- ✅ Консистентность во всем проекте
- ✅ Легкость поддержки и развития

**Статус: ГОТОВО К ИСПОЛЬЗОВАНИЮ** 🎉
