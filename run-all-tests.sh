#!/bin/bash

# Скрипт для запуска всех тестов M2 Middleware
# Использование: ./run-all-tests.sh

echo "╔════════════════════════════════════════════════════════════════╗"
echo "║  M2 Middleware - Запуск всех тестов                           ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""

# Счетчики
TOTAL=0
PASSED=0
FAILED=0

# Функция для запуска теста
run_test() {
    local test_file=$1
    local test_name=$2
    
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "🧪 Запуск: $test_name"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    
    TOTAL=$((TOTAL + 1))
    
    if node "$test_file" > /dev/null 2>&1; then
        echo "✅ ПРОЙДЕН: $test_name"
        PASSED=$((PASSED + 1))
    else
        echo "❌ ПРОВАЛЕН: $test_name"
        FAILED=$((FAILED + 1))
        echo "   Запустите 'node $test_file' для деталей"
    fi
    echo ""
}

# Основные компоненты
echo "═══ Основные компоненты ═══"
echo ""
run_test "test-mapper.js" "MapperService"
run_test "test-stock-service.js" "StockService"
run_test "test-order-service.js" "OrderService"

# Инфраструктура
echo "═══ Инфраструктура ═══"
echo ""
run_test "test-logging.js" "Система логирования"
run_test "test-cron-scheduler.js" "CronScheduler"
run_test "test-webhook-simple.js" "Webhook endpoint"

# Устойчивость
echo "═══ Устойчивость и надежность ═══"
echo ""
run_test "test-resilience.js" "Устойчивость системы"

# Интеграционные тесты
echo "═══ Интеграционные тесты ═══"
echo ""
run_test "test-mapper-integration.js" "Интеграция MapperService"
run_test "test-mapper-edge-cases.js" "Граничные случаи MapperService"
run_test "test-integration-e2e.js" "End-to-End интеграция (product.id → offerId)"

# Финальный отчет
echo "╔════════════════════════════════════════════════════════════════╗"
echo "║  Результаты тестирования                                      ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""
echo "Всего тестов:    $TOTAL"
echo "Пройдено:        $PASSED"
echo "Провалено:       $FAILED"
echo ""

if [ $FAILED -eq 0 ]; then
    echo "✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!"
    echo ""
    echo "Система готова к использованию."
    exit 0
else
    echo "❌ НЕКОТОРЫЕ ТЕСТЫ ПРОВАЛЕНЫ"
    echo ""
    echo "Пожалуйста, проверьте проваленные тесты."
    exit 1
fi
